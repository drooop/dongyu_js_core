---
name: grill-me
description: Repository-scoped entry point for explicitly stress-testing a high-impact plan, quotation or external commitment, architecture decision, or SSOT change before approval. Do not use for routine small tasks or already-approved execution.
---

Resolve and read the sibling `../grilling/SKILL.md`, then invoke `$grilling` and run its interview workflow.

If `../grilling/SKILL.md` is missing or unreadable, stop and report the missing dependency. Do not silently fall back to an improvised review.

This wrapper is explicit-only. It does not approve an iteration, authorize implementation, or replace the repository Phase Gate. Finish only after the user confirms shared understanding and the confirmed review packet has been persisted in the registered iteration artifacts.
