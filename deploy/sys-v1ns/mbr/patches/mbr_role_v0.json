{
  "version": "mt.v0",
  "op_id": "mbr_role_v0_seed",
  "records": [
    {
      "op": "create_model",
      "model_id": -10,
      "name": "system",
      "type": "system"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "sys_worker_id",
      "t": "worker.id",
      "v": "5/10/28/35/14"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "sys_worker_role",
      "t": "worker.role",
      "v": "DEM"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_base",
      "t": "str",
      "v": "UIPUT/ws/dam/pic/de/sw"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_cb_in",
      "t": "pin.bus.cb.in",
      "v": null
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_cb_in_wiring",
      "t": "pin.connect.label",
      "v": [
        {
          "from": "mbr_cb_in",
          "to": [
            "mbr_cb_dispatch:in"
          ]
        }
      ]
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_cb_dispatch",
      "t": "func.js",
      "v": {
        "code": "const records = Array.isArray(label && label.v) ? label.v : [];\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst isRecord = (record) => {\n  if (!record || typeof record !== 'object' || Array.isArray(record)) return false;\n  const keys = Object.keys(record).sort().join(',');\n  return keys === 'c,id,k,p,r,t,v'\n    && Number.isInteger(record.id)\n    && Number.isInteger(record.p)\n    && Number.isInteger(record.r)\n    && Number.isInteger(record.c)\n    && typeof record.k === 'string'\n    && record.k.length > 0\n    && typeof record.t === 'string'\n    && record.t.length > 0\n    && hasOwn(record, 'v');\n};\nconst payloadRecord = (key) => records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === key) || null;\nconst payloadString = (key) => { const record = payloadRecord(key); return record && record.t === 'str' && typeof record.v === 'string' && record.v.trim() === record.v ? record.v : ''; };\nconst payloadInt = (key) => { const record = payloadRecord(key); return record && record.t === 'int' && Number.isInteger(record.v) ? record.v : null; };\nconst payloadJson = (key) => { const record = payloadRecord(key); return record && record.t === 'json' ? record.v : null; };\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst safeTopic = (value) => {\n  if (typeof value !== 'string' || value.trim() !== value || value.length === 0) return false;\n  const parts = value.split('/');\n  return parts.length === 9 && parts[0] === 'UIPUT' && parts.every(safeSegment) && /^[1-9][0-9]*$/.test(parts[7]);\n};\nconst legacyKeys = new Set(['source_model_id', 'pin', 'route', 'reply_to', 'route.reply_to', 'return_topic', 'returnTopic', 'result_topic']);\nconst valueHasLegacy = (value, seen = new WeakSet()) => {\n  if (!value) return false;\n  if (Array.isArray(value)) {\n    if (seen.has(value)) return false;\n    seen.add(value);\n    return value.some((item) => valueHasLegacy(item, seen));\n  }\n  if (typeof value !== 'object') return false;\n  if (seen.has(value)) return false;\n  seen.add(value);\n  for (const [key, child] of Object.entries(value)) {\n    if (legacyKeys.has(key)) return true;\n    if (key === 'k' && typeof child === 'string' && legacyKeys.has(child)) return true;\n    if (valueHasLegacy(child, seen)) return true;\n  }\n  return false;\n};\nconst isSlideAppBundleResponsePayload = (records) => {\n  if (!Array.isArray(records) || records.length === 0 || !records.every(isRecord)) return false;\n  const kind = records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === '__mt_payload_kind');\n  return kind && kind.t === 'str' && kind.v === 'slide_app_bundle_response.v1';\n};\nconst recordsHaveLegacy = (records, options = {}) => {\n  if (!Array.isArray(records) || records.length === 0) return true;\n  if (!records.every(isRecord)) return true;\n  const allowSlideAppBundlePayload = options.allowSlideAppBundlePayload === true && isSlideAppBundleResponsePayload(records);\n  return records.some((record) => {\n    if (legacyKeys.has(record.k)) return true;\n    if (allowSlideAppBundlePayload && record.k === 'bundle_payload') {\n      if (!Array.isArray(record.v) || record.v.length === 0 || !record.v.every(isRecord)) return true;\n      return record.v.some((bundleRecord) => legacyKeys.has(bundleRecord.k));\n    }\n    return valueHasLegacy(record.v);\n  });\n};\nconst fail = (detail) => {\n  V1N.addLabel('mbr_cb_error', 'json', { op_id: payloadString('op_id') || payloadString('__mt_request_id'), code: 'invalid_control_bus_payload', detail, ts: Date.now() });\n};\nif (records.length === 0 || !records.every(isRecord)) { fail('temporary_modeltable_required'); return; }\nfor (const record of records) {\n  if (legacyKeys.has(record.k) || (record.k !== 'payload' && valueHasLegacy(record.v))) { fail('legacy_pin_payload_metadata_removed'); return; }\n}\nconst duplicateKeys = new Set(['__mt_payload_kind', '__mt_request_id', 'op_id', 'message_role', 'topic', 'route_kind', 'endpoint_worker_id', 'endpoint_model_id', 'endpoint_pin', 'origin_worker_id', 'origin_model_id', 'origin_pin', 'reply_target_worker_id', 'reply_target_model_id', 'reply_target_pin', 'payload', 'timestamp', 'bus_out_key', 'bus']);\nconst seen = new Set();\nfor (const record of records) {\n  if (!duplicateKeys.has(record.k)) continue;\n  if (seen.has(record.k)) { fail('invalid_pin_payload_records'); return; }\n  seen.add(record.k);\n}\nif (payloadString('__mt_payload_kind') !== 'pin_payload.v1') { fail('invalid_payload_kind'); return; }\nconst role = payloadString('message_role');\nif (role !== 'request' && role !== 'response') { fail('invalid_message_role'); return; }\nconst endpointOk = safeSegment(payloadString('endpoint_worker_id')) && Number.isInteger(payloadInt('endpoint_model_id')) && payloadInt('endpoint_model_id') > 0 && safeSegment(payloadString('endpoint_pin'));\nconst originOk = safeSegment(payloadString('origin_worker_id')) && Number.isInteger(payloadInt('origin_model_id')) && payloadInt('origin_model_id') > 0 && safeSegment(payloadString('origin_pin'));\nconst replyOk = safeSegment(payloadString('reply_target_worker_id')) && Number.isInteger(payloadInt('reply_target_model_id')) && payloadInt('reply_target_model_id') > 0 && safeSegment(payloadString('reply_target_pin'));\nconst nestedPayload = payloadJson('payload');\nif (!endpointOk || !originOk || !replyOk || !Array.isArray(nestedPayload) || !nestedPayload.every(isRecord)) { fail('invalid_pin_payload_records'); return; }\nif (recordsHaveLegacy(nestedPayload, { allowSlideAppBundlePayload: true })) { fail('legacy_pin_payload_metadata_removed'); return; }\nconst topic = payloadString('topic');\nif (!safeTopic(topic)) { fail('missing_or_invalid_topic'); return; }\nconst routeKindRecord = payloadRecord('route_kind');\nconst routeKind = routeKindRecord ? payloadString('route_kind') : 'control';\nif (routeKind !== 'control' && routeKind !== 'management') { fail('invalid_route_kind'); return; }\nif (routeKind === 'management') {\n  V1N.addLabel('mbr_mb_out', 'pin.bus.mb.out', records);\n} else {\n  V1N.addLabel('mbr_cb_out', 'pin.bus.cb.out', records);\n}",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_cb_out",
      "t": "pin.bus.cb.out",
      "v": null
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mb_out",
      "t": "pin.bus.mb.out",
      "v": null
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 1,
      "r": 0,
      "c": 0,
      "k": "model_type",
      "t": "model.submt",
      "v": -10
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_heartbeat_interval_ms",
      "t": "int",
      "v": 30000
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_matrix_event_filter",
      "t": "str",
      "v": "pin_payload"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_matrix_inbox_label",
      "t": "str",
      "v": "mbr_mgmt_inbox"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_matrix_func",
      "t": "str",
      "v": "mbr_mgmt_dispatch"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mgmt_dispatch",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst isRecord = (record) => {\n  if (!record || typeof record !== 'object' || Array.isArray(record)) return false;\n  const keys = Object.keys(record).sort().join(',');\n  return keys === 'c,id,k,p,r,t,v'\n    && Number.isInteger(record.id)\n    && Number.isInteger(record.p)\n    && Number.isInteger(record.r)\n    && Number.isInteger(record.c)\n    && typeof record.k === 'string'\n    && record.k.length > 0\n    && typeof record.t === 'string'\n    && record.t.length > 0\n    && hasOwn(record, 'v');\n};\nconst payloadRecord = (records, key) => Array.isArray(records) ? records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === key) || null : null;\nconst payloadString = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'str' && typeof record.v === 'string' && record.v.trim() === record.v ? record.v : ''; };\nconst payloadInt = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'int' && Number.isInteger(record.v) ? record.v : null; };\nconst payloadJson = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'json' ? record.v : null; };\nconst legacyKeys = new Set(['source_model_id', 'pin', 'route', 'reply_to', 'route.reply_to', 'return_topic', 'returnTopic', 'result_topic']);\nconst valueHasLegacy = (value, seen = new WeakSet()) => {\n  if (!value) return false;\n  if (Array.isArray(value)) {\n    if (seen.has(value)) return false;\n    seen.add(value);\n    return value.some((item) => valueHasLegacy(item, seen));\n  }\n  if (typeof value !== 'object') return false;\n  if (seen.has(value)) return false;\n  seen.add(value);\n  for (const [key, child] of Object.entries(value)) {\n    if (legacyKeys.has(key)) return true;\n    if (key === 'k' && typeof child === 'string' && legacyKeys.has(child)) return true;\n    if (valueHasLegacy(child, seen)) return true;\n  }\n  return false;\n};\nconst isSlideAppBundleResponsePayload = (records) => {\n  if (!Array.isArray(records) || records.length === 0 || !records.every(isRecord)) return false;\n  const kind = records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === '__mt_payload_kind');\n  return kind && kind.t === 'str' && kind.v === 'slide_app_bundle_response.v1';\n};\nconst recordsHaveLegacy = (records, options = {}) => {\n  if (!Array.isArray(records) || records.length === 0) return true;\n  if (!records.every(isRecord)) return true;\n  const allowSlideAppBundlePayload = options.allowSlideAppBundlePayload === true && isSlideAppBundleResponsePayload(records);\n  return records.some((record) => {\n    if (legacyKeys.has(record.k)) return true;\n    if (allowSlideAppBundlePayload && record.k === 'bundle_payload') {\n      if (!Array.isArray(record.v) || record.v.length === 0 || !record.v.every(isRecord)) return true;\n      return record.v.some((bundleRecord) => legacyKeys.has(bundleRecord.k));\n    }\n    return valueHasLegacy(record.v);\n  });\n};\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst safeTopic = (value) => {\n  if (typeof value !== 'string' || value.trim() !== value || value.length === 0) return false;\n  const parts = value.split('/');\n  return parts.length === 9 && parts[0] === 'UIPUT' && parts.every(safeSegment) && /^[1-9][0-9]*$/.test(parts[7]);\n};\nconst validRouteKind = (value) => value === 'control' || value === 'management';\nconst endpointFrom = (records, prefix) => {\n  const endpoint = {\n    worker_id: payloadString(records, prefix + '_worker_id'),\n    model_id: payloadInt(records, prefix + '_model_id'),\n    pin: payloadString(records, prefix + '_pin'),\n  };\n  if (!safeSegment(endpoint.worker_id) || !Number.isInteger(endpoint.model_id) || endpoint.model_id <= 0 || !safeSegment(endpoint.pin)) return null;\n  return endpoint;\n};\nconst hasDuplicate = (records, keys) => {\n  const watched = new Set(keys);\n  const seen = new Set();\n  for (const record of records) {\n    if (!record || !watched.has(record.k)) continue;\n    if (seen.has(record.k)) return true;\n    seen.add(record.k);\n  }\n  return false;\n};\nconst parsePinPayload = (packet) => {\n  if (!packet || typeof packet !== 'object' || Array.isArray(packet)) return { ok: false, code: 'invalid_packet' };\n  const packetKeys = Object.keys(packet).sort().join(',');\n  if (packetKeys !== 'payload,type,version') return { ok: false, code: 'loose_pin_payload_fields_removed' };\n  if (packet.version !== 'v1' || packet.type !== 'pin_payload' || !Array.isArray(packet.payload)) return { ok: false, code: 'invalid_packet' };\n  const records = packet.payload;\n  if (records.length === 0 || !records.every(isRecord)) return { ok: false, code: 'temporary_modeltable_required' };\n  const metadataKeys = ['__mt_payload_kind', '__mt_request_id', 'op_id', 'message_role', 'topic', 'route_kind', 'endpoint_worker_id', 'endpoint_model_id', 'endpoint_pin', 'origin_worker_id', 'origin_model_id', 'origin_pin', 'reply_target_worker_id', 'reply_target_model_id', 'reply_target_pin', 'payload', 'timestamp', 'bus_out_key', 'bus'];\n  if (hasDuplicate(records, metadataKeys)) return { ok: false, code: 'invalid_pin_payload_records' };\n  for (const record of records) {\n    if (legacyKeys.has(record.k)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n    if (record.k !== 'payload' && valueHasLegacy(record.v)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  }\n  const kind = payloadString(records, '__mt_payload_kind');\n  if (kind !== 'pin_payload.v1') return { ok: false, code: 'invalid_payload_kind' };\n  const requestId = payloadString(records, '__mt_request_id');\n  const opId = payloadString(records, 'op_id') || requestId;\n  if (!opId) return { ok: false, code: 'missing_request_correlation' };\n  const messageRole = payloadString(records, 'message_role');\n  if (messageRole !== 'request' && messageRole !== 'response') return { ok: false, code: 'invalid_message_role' };\n  const topic = payloadString(records, 'topic');\n  const routeKind = payloadString(records, 'route_kind') || 'control';\n  const endpoint = endpointFrom(records, 'endpoint');\n  const origin = endpointFrom(records, 'origin');\n  const replyTarget = endpointFrom(records, 'reply_target');\n  const nestedPayload = payloadJson(records, 'payload');\n  if (!endpoint || !origin || !replyTarget || !safeTopic(topic) || !validRouteKind(routeKind) || !Array.isArray(nestedPayload) || nestedPayload.length === 0 || !nestedPayload.every(isRecord)) return { ok: false, code: 'invalid_pin_payload_records' };\n  if (recordsHaveLegacy(nestedPayload, { allowSlideAppBundlePayload: true })) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  return { ok: true, records, endpoint, origin, replyTarget, nestedPayload, opId, messageRole, topic, routeKind };\n};\nconst pinBusValue = ({ opId, endpoint, origin, replyTarget, payload, timestamp = Date.now(), bus = '', messageRole = 'request' }) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', opId),\n    mt('op_id', 'str', opId),\n    mt('message_role', 'str', messageRole),\n    mt('endpoint_worker_id', 'str', endpoint.worker_id),\n    mt('endpoint_model_id', 'int', endpoint.model_id),\n    mt('endpoint_pin', 'str', endpoint.pin),\n    mt('origin_worker_id', 'str', origin.worker_id),\n    mt('origin_model_id', 'int', origin.model_id),\n    mt('origin_pin', 'str', origin.pin),\n    mt('reply_target_worker_id', 'str', replyTarget.worker_id),\n    mt('reply_target_model_id', 'int', replyTarget.model_id),\n    mt('reply_target_pin', 'str', replyTarget.pin),\n    mt('payload', 'json', payload),\n    mt('timestamp', 'int', timestamp),\n  ];\n  if (bus) records.push(mt('bus', 'str', bus));\n  return records;\n};\nconst writeControlOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', records);\nconst writeMgmtOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', records);\nconst inbox = mtRead(-10, 0, 0, 0, 'mbr_mgmt_inbox');\nif (!inbox || typeof inbox !== 'object' || Array.isArray(inbox)) return;\nconst cleanup = () => { mtDrop(-10, 0, 0, 0, 'mbr_mgmt_inbox'); };\nconst reject = (detail, opId = '') => { mtAdd(-10, 0, 0, 0, 'mbr_mgmt_error', 'json', { op_id: opId, code: 'direct_model_mutation_disabled', detail }); cleanup(); };\nconst parsed = parsePinPayload(inbox);\nif (!parsed.ok) { reject(parsed.code); return; }\nif (parsed.messageRole !== 'request') { cleanup(); return; }\nmtAdd(-10, 0, 0, 0, 'run_mbr_mgmt_to_mqtt', 'str', '1');",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mqtt_inbox_label",
      "t": "str",
      "v": "mbr_mqtt_inbox"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mqtt_func",
      "t": "str",
      "v": "mbr_mqtt_to_mgmt"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_ready_func",
      "t": "str",
      "v": "mbr_ready"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_heartbeat_func",
      "t": "str",
      "v": "mbr_heartbeat"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mgmt_to_mqtt",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst isRecord = (record) => {\n  if (!record || typeof record !== 'object' || Array.isArray(record)) return false;\n  const keys = Object.keys(record).sort().join(',');\n  return keys === 'c,id,k,p,r,t,v'\n    && Number.isInteger(record.id)\n    && Number.isInteger(record.p)\n    && Number.isInteger(record.r)\n    && Number.isInteger(record.c)\n    && typeof record.k === 'string'\n    && record.k.length > 0\n    && typeof record.t === 'string'\n    && record.t.length > 0\n    && hasOwn(record, 'v');\n};\nconst payloadRecord = (records, key) => Array.isArray(records) ? records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === key) || null : null;\nconst payloadString = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'str' && typeof record.v === 'string' && record.v.trim() === record.v ? record.v : ''; };\nconst payloadInt = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'int' && Number.isInteger(record.v) ? record.v : null; };\nconst payloadJson = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'json' ? record.v : null; };\nconst legacyKeys = new Set(['source_model_id', 'pin', 'route', 'reply_to', 'route.reply_to', 'return_topic', 'returnTopic', 'result_topic']);\nconst valueHasLegacy = (value, seen = new WeakSet()) => {\n  if (!value) return false;\n  if (Array.isArray(value)) {\n    if (seen.has(value)) return false;\n    seen.add(value);\n    return value.some((item) => valueHasLegacy(item, seen));\n  }\n  if (typeof value !== 'object') return false;\n  if (seen.has(value)) return false;\n  seen.add(value);\n  for (const [key, child] of Object.entries(value)) {\n    if (legacyKeys.has(key)) return true;\n    if (key === 'k' && typeof child === 'string' && legacyKeys.has(child)) return true;\n    if (valueHasLegacy(child, seen)) return true;\n  }\n  return false;\n};\nconst isSlideAppBundleResponsePayload = (records) => {\n  if (!Array.isArray(records) || records.length === 0 || !records.every(isRecord)) return false;\n  const kind = records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === '__mt_payload_kind');\n  return kind && kind.t === 'str' && kind.v === 'slide_app_bundle_response.v1';\n};\nconst recordsHaveLegacy = (records, options = {}) => {\n  if (!Array.isArray(records) || records.length === 0) return true;\n  if (!records.every(isRecord)) return true;\n  const allowSlideAppBundlePayload = options.allowSlideAppBundlePayload === true && isSlideAppBundleResponsePayload(records);\n  return records.some((record) => {\n    if (legacyKeys.has(record.k)) return true;\n    if (allowSlideAppBundlePayload && record.k === 'bundle_payload') {\n      if (!Array.isArray(record.v) || record.v.length === 0 || !record.v.every(isRecord)) return true;\n      return record.v.some((bundleRecord) => legacyKeys.has(bundleRecord.k));\n    }\n    return valueHasLegacy(record.v);\n  });\n};\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst safeTopic = (value) => {\n  if (typeof value !== 'string' || value.trim() !== value || value.length === 0) return false;\n  const parts = value.split('/');\n  return parts.length === 9 && parts[0] === 'UIPUT' && parts.every(safeSegment) && /^[1-9][0-9]*$/.test(parts[7]);\n};\nconst validRouteKind = (value) => value === 'control' || value === 'management';\nconst endpointFrom = (records, prefix) => {\n  const endpoint = {\n    worker_id: payloadString(records, prefix + '_worker_id'),\n    model_id: payloadInt(records, prefix + '_model_id'),\n    pin: payloadString(records, prefix + '_pin'),\n  };\n  if (!safeSegment(endpoint.worker_id) || !Number.isInteger(endpoint.model_id) || endpoint.model_id <= 0 || !safeSegment(endpoint.pin)) return null;\n  return endpoint;\n};\nconst hasDuplicate = (records, keys) => {\n  const watched = new Set(keys);\n  const seen = new Set();\n  for (const record of records) {\n    if (!record || !watched.has(record.k)) continue;\n    if (seen.has(record.k)) return true;\n    seen.add(record.k);\n  }\n  return false;\n};\nconst parsePinPayload = (packet) => {\n  if (!packet || typeof packet !== 'object' || Array.isArray(packet)) return { ok: false, code: 'invalid_packet' };\n  const packetKeys = Object.keys(packet).sort().join(',');\n  if (packetKeys !== 'payload,type,version') return { ok: false, code: 'loose_pin_payload_fields_removed' };\n  if (packet.version !== 'v1' || packet.type !== 'pin_payload' || !Array.isArray(packet.payload)) return { ok: false, code: 'invalid_packet' };\n  const records = packet.payload;\n  if (records.length === 0 || !records.every(isRecord)) return { ok: false, code: 'temporary_modeltable_required' };\n  const metadataKeys = ['__mt_payload_kind', '__mt_request_id', 'op_id', 'message_role', 'topic', 'route_kind', 'endpoint_worker_id', 'endpoint_model_id', 'endpoint_pin', 'origin_worker_id', 'origin_model_id', 'origin_pin', 'reply_target_worker_id', 'reply_target_model_id', 'reply_target_pin', 'payload', 'timestamp', 'bus_out_key', 'bus'];\n  if (hasDuplicate(records, metadataKeys)) return { ok: false, code: 'invalid_pin_payload_records' };\n  for (const record of records) {\n    if (legacyKeys.has(record.k)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n    if (record.k !== 'payload' && valueHasLegacy(record.v)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  }\n  const kind = payloadString(records, '__mt_payload_kind');\n  if (kind !== 'pin_payload.v1') return { ok: false, code: 'invalid_payload_kind' };\n  const requestId = payloadString(records, '__mt_request_id');\n  const opId = payloadString(records, 'op_id') || requestId;\n  if (!opId) return { ok: false, code: 'missing_request_correlation' };\n  const messageRole = payloadString(records, 'message_role');\n  if (messageRole !== 'request' && messageRole !== 'response') return { ok: false, code: 'invalid_message_role' };\n  const topic = payloadString(records, 'topic');\n  const routeKind = payloadString(records, 'route_kind') || 'control';\n  const endpoint = endpointFrom(records, 'endpoint');\n  const origin = endpointFrom(records, 'origin');\n  const replyTarget = endpointFrom(records, 'reply_target');\n  const nestedPayload = payloadJson(records, 'payload');\n  if (!endpoint || !origin || !replyTarget || !safeTopic(topic) || !validRouteKind(routeKind) || !Array.isArray(nestedPayload) || nestedPayload.length === 0 || !nestedPayload.every(isRecord)) return { ok: false, code: 'invalid_pin_payload_records' };\n  if (recordsHaveLegacy(nestedPayload, { allowSlideAppBundlePayload: true })) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  return { ok: true, records, endpoint, origin, replyTarget, nestedPayload, opId, messageRole, topic, routeKind };\n};\nconst pinBusValue = ({ opId, endpoint, origin, replyTarget, payload, timestamp = Date.now(), bus = '', messageRole = 'request' }) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', opId),\n    mt('op_id', 'str', opId),\n    mt('message_role', 'str', messageRole),\n    mt('endpoint_worker_id', 'str', endpoint.worker_id),\n    mt('endpoint_model_id', 'int', endpoint.model_id),\n    mt('endpoint_pin', 'str', endpoint.pin),\n    mt('origin_worker_id', 'str', origin.worker_id),\n    mt('origin_model_id', 'int', origin.model_id),\n    mt('origin_pin', 'str', origin.pin),\n    mt('reply_target_worker_id', 'str', replyTarget.worker_id),\n    mt('reply_target_model_id', 'int', replyTarget.model_id),\n    mt('reply_target_pin', 'str', replyTarget.pin),\n    mt('payload', 'json', payload),\n    mt('timestamp', 'int', timestamp),\n  ];\n  if (bus) records.push(mt('bus', 'str', bus));\n  return records;\n};\nconst writeControlOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', records);\nconst writeMgmtOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', records);\nconst inbox = mtRead(-10, 0, 0, 0, 'mbr_mgmt_inbox');\nif (!inbox || typeof inbox !== 'object' || Array.isArray(inbox)) return;\nconst cleanup = () => { mtDrop(-10, 0, 0, 0, 'mbr_mgmt_inbox'); };\nconst reject = (detail, opId = '') => { mtAdd(-10, 0, 0, 0, 'mbr_mgmt_error', 'json', { op_id: opId, code: 'direct_model_mutation_disabled', detail }); cleanup(); };\nconst parsed = parsePinPayload(inbox);\nif (!parsed.ok) { reject(parsed.code); return; }\nif (parsed.messageRole !== 'request') { cleanup(); return; }\nif (parsed.endpoint.pin.includes(':')) { reject('function_endpoint_not_allowed', parsed.opId); return; }\nwriteControlOut(parsed.records);\ncleanup();",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mqtt_to_mgmt",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst isRecord = (record) => {\n  if (!record || typeof record !== 'object' || Array.isArray(record)) return false;\n  const keys = Object.keys(record).sort().join(',');\n  return keys === 'c,id,k,p,r,t,v'\n    && Number.isInteger(record.id)\n    && Number.isInteger(record.p)\n    && Number.isInteger(record.r)\n    && Number.isInteger(record.c)\n    && typeof record.k === 'string'\n    && record.k.length > 0\n    && typeof record.t === 'string'\n    && record.t.length > 0\n    && hasOwn(record, 'v');\n};\nconst payloadRecord = (records, key) => Array.isArray(records) ? records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === key) || null : null;\nconst payloadString = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'str' && typeof record.v === 'string' && record.v.trim() === record.v ? record.v : ''; };\nconst payloadInt = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'int' && Number.isInteger(record.v) ? record.v : null; };\nconst payloadJson = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'json' ? record.v : null; };\nconst legacyKeys = new Set(['source_model_id', 'pin', 'route', 'reply_to', 'route.reply_to', 'return_topic', 'returnTopic', 'result_topic']);\nconst valueHasLegacy = (value, seen = new WeakSet()) => {\n  if (!value) return false;\n  if (Array.isArray(value)) {\n    if (seen.has(value)) return false;\n    seen.add(value);\n    return value.some((item) => valueHasLegacy(item, seen));\n  }\n  if (typeof value !== 'object') return false;\n  if (seen.has(value)) return false;\n  seen.add(value);\n  for (const [key, child] of Object.entries(value)) {\n    if (legacyKeys.has(key)) return true;\n    if (key === 'k' && typeof child === 'string' && legacyKeys.has(child)) return true;\n    if (valueHasLegacy(child, seen)) return true;\n  }\n  return false;\n};\nconst isSlideAppBundleResponsePayload = (records) => {\n  if (!Array.isArray(records) || records.length === 0 || !records.every(isRecord)) return false;\n  const kind = records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === '__mt_payload_kind');\n  return kind && kind.t === 'str' && kind.v === 'slide_app_bundle_response.v1';\n};\nconst recordsHaveLegacy = (records, options = {}) => {\n  if (!Array.isArray(records) || records.length === 0) return true;\n  if (!records.every(isRecord)) return true;\n  const allowSlideAppBundlePayload = options.allowSlideAppBundlePayload === true && isSlideAppBundleResponsePayload(records);\n  return records.some((record) => {\n    if (legacyKeys.has(record.k)) return true;\n    if (allowSlideAppBundlePayload && record.k === 'bundle_payload') {\n      if (!Array.isArray(record.v) || record.v.length === 0 || !record.v.every(isRecord)) return true;\n      return record.v.some((bundleRecord) => legacyKeys.has(bundleRecord.k));\n    }\n    return valueHasLegacy(record.v);\n  });\n};\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst safeTopic = (value) => {\n  if (typeof value !== 'string' || value.trim() !== value || value.length === 0) return false;\n  const parts = value.split('/');\n  return parts.length === 9 && parts[0] === 'UIPUT' && parts.every(safeSegment) && /^[1-9][0-9]*$/.test(parts[7]);\n};\nconst validRouteKind = (value) => value === 'control' || value === 'management';\nconst endpointFrom = (records, prefix) => {\n  const endpoint = {\n    worker_id: payloadString(records, prefix + '_worker_id'),\n    model_id: payloadInt(records, prefix + '_model_id'),\n    pin: payloadString(records, prefix + '_pin'),\n  };\n  if (!safeSegment(endpoint.worker_id) || !Number.isInteger(endpoint.model_id) || endpoint.model_id <= 0 || !safeSegment(endpoint.pin)) return null;\n  return endpoint;\n};\nconst hasDuplicate = (records, keys) => {\n  const watched = new Set(keys);\n  const seen = new Set();\n  for (const record of records) {\n    if (!record || !watched.has(record.k)) continue;\n    if (seen.has(record.k)) return true;\n    seen.add(record.k);\n  }\n  return false;\n};\nconst parsePinPayload = (packet) => {\n  if (!packet || typeof packet !== 'object' || Array.isArray(packet)) return { ok: false, code: 'invalid_packet' };\n  const packetKeys = Object.keys(packet).sort().join(',');\n  if (packetKeys !== 'payload,type,version') return { ok: false, code: 'loose_pin_payload_fields_removed' };\n  if (packet.version !== 'v1' || packet.type !== 'pin_payload' || !Array.isArray(packet.payload)) return { ok: false, code: 'invalid_packet' };\n  const records = packet.payload;\n  if (records.length === 0 || !records.every(isRecord)) return { ok: false, code: 'temporary_modeltable_required' };\n  const metadataKeys = ['__mt_payload_kind', '__mt_request_id', 'op_id', 'message_role', 'topic', 'route_kind', 'endpoint_worker_id', 'endpoint_model_id', 'endpoint_pin', 'origin_worker_id', 'origin_model_id', 'origin_pin', 'reply_target_worker_id', 'reply_target_model_id', 'reply_target_pin', 'payload', 'timestamp', 'bus_out_key', 'bus'];\n  if (hasDuplicate(records, metadataKeys)) return { ok: false, code: 'invalid_pin_payload_records' };\n  for (const record of records) {\n    if (legacyKeys.has(record.k)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n    if (record.k !== 'payload' && valueHasLegacy(record.v)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  }\n  const kind = payloadString(records, '__mt_payload_kind');\n  if (kind !== 'pin_payload.v1') return { ok: false, code: 'invalid_payload_kind' };\n  const requestId = payloadString(records, '__mt_request_id');\n  const opId = payloadString(records, 'op_id') || requestId;\n  if (!opId) return { ok: false, code: 'missing_request_correlation' };\n  const messageRole = payloadString(records, 'message_role');\n  if (messageRole !== 'request' && messageRole !== 'response') return { ok: false, code: 'invalid_message_role' };\n  const topic = payloadString(records, 'topic');\n  const routeKind = payloadString(records, 'route_kind') || 'control';\n  const endpoint = endpointFrom(records, 'endpoint');\n  const origin = endpointFrom(records, 'origin');\n  const replyTarget = endpointFrom(records, 'reply_target');\n  const nestedPayload = payloadJson(records, 'payload');\n  if (!endpoint || !origin || !replyTarget || !safeTopic(topic) || !validRouteKind(routeKind) || !Array.isArray(nestedPayload) || nestedPayload.length === 0 || !nestedPayload.every(isRecord)) return { ok: false, code: 'invalid_pin_payload_records' };\n  if (recordsHaveLegacy(nestedPayload, { allowSlideAppBundlePayload: true })) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  return { ok: true, records, endpoint, origin, replyTarget, nestedPayload, opId, messageRole, topic, routeKind };\n};\nconst pinBusValue = ({ opId, endpoint, origin, replyTarget, payload, timestamp = Date.now(), bus = '', messageRole = 'request' }) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', opId),\n    mt('op_id', 'str', opId),\n    mt('message_role', 'str', messageRole),\n    mt('endpoint_worker_id', 'str', endpoint.worker_id),\n    mt('endpoint_model_id', 'int', endpoint.model_id),\n    mt('endpoint_pin', 'str', endpoint.pin),\n    mt('origin_worker_id', 'str', origin.worker_id),\n    mt('origin_model_id', 'int', origin.model_id),\n    mt('origin_pin', 'str', origin.pin),\n    mt('reply_target_worker_id', 'str', replyTarget.worker_id),\n    mt('reply_target_model_id', 'int', replyTarget.model_id),\n    mt('reply_target_pin', 'str', replyTarget.pin),\n    mt('payload', 'json', payload),\n    mt('timestamp', 'int', timestamp),\n  ];\n  if (bus) records.push(mt('bus', 'str', bus));\n  return records;\n};\nconst writeControlOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', records);\nconst writeMgmtOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', records);\nconst inbox = mtRead(-10, 0, 0, 0, 'mbr_mqtt_inbox');\nif (!inbox || typeof inbox !== 'object' || Array.isArray(inbox)) return;\nconst payload = inbox.payload;\nconst cleanup = () => { mtDrop(-10, 0, 0, 0, 'mbr_mqtt_inbox'); };\nconst parsed = parsePinPayload(payload);\nconst reject = (detail) => {\n  mtAdd(-10, 0, 0, 0, 'mbr_mqtt_error', 'json', { op_id: parsed && parsed.opId ? parsed.opId : '', code: 'invalid_mqtt_pin_payload', detail });\n  cleanup();\n};\nif (!parsed.ok) { reject(parsed.code); return; }\nif (parsed.endpoint.pin.includes(':')) { reject('function_endpoint_not_allowed'); return; }\nif (parsed.messageRole !== 'response') { cleanup(); return; }\nif (parsed.routeKind === 'management') {\n  writeMgmtOut(parsed.records);\n} else {\n  writeControlOut(parsed.records);\n}\ncleanup();",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_heartbeat",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst isRecord = (record) => {\n  if (!record || typeof record !== 'object' || Array.isArray(record)) return false;\n  const keys = Object.keys(record).sort().join(',');\n  return keys === 'c,id,k,p,r,t,v'\n    && Number.isInteger(record.id)\n    && Number.isInteger(record.p)\n    && Number.isInteger(record.r)\n    && Number.isInteger(record.c)\n    && typeof record.k === 'string'\n    && record.k.length > 0\n    && typeof record.t === 'string'\n    && record.t.length > 0\n    && hasOwn(record, 'v');\n};\nconst payloadRecord = (records, key) => Array.isArray(records) ? records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === key) || null : null;\nconst payloadString = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'str' && typeof record.v === 'string' && record.v.trim() === record.v ? record.v : ''; };\nconst payloadInt = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'int' && Number.isInteger(record.v) ? record.v : null; };\nconst payloadJson = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'json' ? record.v : null; };\nconst legacyKeys = new Set(['source_model_id', 'pin', 'route', 'reply_to', 'route.reply_to', 'return_topic', 'returnTopic', 'result_topic']);\nconst valueHasLegacy = (value, seen = new WeakSet()) => {\n  if (!value) return false;\n  if (Array.isArray(value)) {\n    if (seen.has(value)) return false;\n    seen.add(value);\n    return value.some((item) => valueHasLegacy(item, seen));\n  }\n  if (typeof value !== 'object') return false;\n  if (seen.has(value)) return false;\n  seen.add(value);\n  for (const [key, child] of Object.entries(value)) {\n    if (legacyKeys.has(key)) return true;\n    if (key === 'k' && typeof child === 'string' && legacyKeys.has(child)) return true;\n    if (valueHasLegacy(child, seen)) return true;\n  }\n  return false;\n};\nconst isSlideAppBundleResponsePayload = (records) => {\n  if (!Array.isArray(records) || records.length === 0 || !records.every(isRecord)) return false;\n  const kind = records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === '__mt_payload_kind');\n  return kind && kind.t === 'str' && kind.v === 'slide_app_bundle_response.v1';\n};\nconst recordsHaveLegacy = (records, options = {}) => {\n  if (!Array.isArray(records) || records.length === 0) return true;\n  if (!records.every(isRecord)) return true;\n  const allowSlideAppBundlePayload = options.allowSlideAppBundlePayload === true && isSlideAppBundleResponsePayload(records);\n  return records.some((record) => {\n    if (legacyKeys.has(record.k)) return true;\n    if (allowSlideAppBundlePayload && record.k === 'bundle_payload') {\n      if (!Array.isArray(record.v) || record.v.length === 0 || !record.v.every(isRecord)) return true;\n      return record.v.some((bundleRecord) => legacyKeys.has(bundleRecord.k));\n    }\n    return valueHasLegacy(record.v);\n  });\n};\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst endpointFrom = (records, prefix) => {\n  const endpoint = {\n    worker_id: payloadString(records, prefix + '_worker_id'),\n    model_id: payloadInt(records, prefix + '_model_id'),\n    pin: payloadString(records, prefix + '_pin'),\n  };\n  if (!safeSegment(endpoint.worker_id) || !Number.isInteger(endpoint.model_id) || endpoint.model_id <= 0 || !safeSegment(endpoint.pin)) return null;\n  return endpoint;\n};\nconst hasDuplicate = (records, keys) => {\n  const watched = new Set(keys);\n  const seen = new Set();\n  for (const record of records) {\n    if (!record || !watched.has(record.k)) continue;\n    if (seen.has(record.k)) return true;\n    seen.add(record.k);\n  }\n  return false;\n};\nconst parsePinPayload = (packet) => {\n  if (!packet || typeof packet !== 'object' || Array.isArray(packet)) return { ok: false, code: 'invalid_packet' };\n  const packetKeys = Object.keys(packet).sort().join(',');\n  if (packetKeys !== 'payload,type,version') return { ok: false, code: 'loose_pin_payload_fields_removed' };\n  if (packet.version !== 'v1' || packet.type !== 'pin_payload' || !Array.isArray(packet.payload)) return { ok: false, code: 'invalid_packet' };\n  const records = packet.payload;\n  if (records.length === 0 || !records.every(isRecord)) return { ok: false, code: 'temporary_modeltable_required' };\n  const metadataKeys = ['__mt_payload_kind', '__mt_request_id', 'op_id', 'message_role', 'endpoint_worker_id', 'endpoint_model_id', 'endpoint_pin', 'origin_worker_id', 'origin_model_id', 'origin_pin', 'reply_target_worker_id', 'reply_target_model_id', 'reply_target_pin', 'payload', 'timestamp', 'bus_out_key', 'bus'];\n  if (hasDuplicate(records, metadataKeys)) return { ok: false, code: 'invalid_pin_payload_records' };\n  for (const record of records) {\n    if (legacyKeys.has(record.k)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n    if (record.k !== 'payload' && valueHasLegacy(record.v)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  }\n  const kind = payloadString(records, '__mt_payload_kind');\n  if (kind !== 'pin_payload.v1') return { ok: false, code: 'invalid_payload_kind' };\n  const requestId = payloadString(records, '__mt_request_id');\n  const opId = payloadString(records, 'op_id') || requestId;\n  if (!opId) return { ok: false, code: 'missing_request_correlation' };\n  const messageRole = payloadString(records, 'message_role');\n  if (messageRole !== 'request' && messageRole !== 'response') return { ok: false, code: 'invalid_message_role' };\n  const endpoint = endpointFrom(records, 'endpoint');\n  const origin = endpointFrom(records, 'origin');\n  const replyTarget = endpointFrom(records, 'reply_target');\n  const nestedPayload = payloadJson(records, 'payload');\n  if (!endpoint || !origin || !replyTarget || !Array.isArray(nestedPayload) || nestedPayload.length === 0 || !nestedPayload.every(isRecord)) return { ok: false, code: 'invalid_pin_payload_records' };\n  if (recordsHaveLegacy(nestedPayload, { allowSlideAppBundlePayload: true })) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  return { ok: true, records, endpoint, origin, replyTarget, nestedPayload, opId, messageRole };\n};\nconst pinBusValue = ({ opId, endpoint, origin, replyTarget, payload, timestamp = Date.now(), bus = '', messageRole = 'request' }) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', opId),\n    mt('op_id', 'str', opId),\n    mt('message_role', 'str', messageRole),\n    mt('endpoint_worker_id', 'str', endpoint.worker_id),\n    mt('endpoint_model_id', 'int', endpoint.model_id),\n    mt('endpoint_pin', 'str', endpoint.pin),\n    mt('origin_worker_id', 'str', origin.worker_id),\n    mt('origin_model_id', 'int', origin.model_id),\n    mt('origin_pin', 'str', origin.pin),\n    mt('reply_target_worker_id', 'str', replyTarget.worker_id),\n    mt('reply_target_model_id', 'int', replyTarget.model_id),\n    mt('reply_target_pin', 'str', replyTarget.pin),\n    mt('payload', 'json', payload),\n    mt('timestamp', 'int', timestamp),\n  ];\n  if (bus) records.push(mt('bus', 'str', bus));\n  return records;\n};\nconst writeControlOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', records);\nconst writeMgmtOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', records);\nconst opId = 'mbr_heartbeat_' + Date.now();\nmtAdd(-10, 0, 0, 0, 'mbr_heartbeat_status', 'json', { op_id: opId, status: 'ready', ts: Date.now() });",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_ready",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst isRecord = (record) => {\n  if (!record || typeof record !== 'object' || Array.isArray(record)) return false;\n  const keys = Object.keys(record).sort().join(',');\n  return keys === 'c,id,k,p,r,t,v'\n    && Number.isInteger(record.id)\n    && Number.isInteger(record.p)\n    && Number.isInteger(record.r)\n    && Number.isInteger(record.c)\n    && typeof record.k === 'string'\n    && record.k.length > 0\n    && typeof record.t === 'string'\n    && record.t.length > 0\n    && hasOwn(record, 'v');\n};\nconst payloadRecord = (records, key) => Array.isArray(records) ? records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === key) || null : null;\nconst payloadString = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'str' && typeof record.v === 'string' && record.v.trim() === record.v ? record.v : ''; };\nconst payloadInt = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'int' && Number.isInteger(record.v) ? record.v : null; };\nconst payloadJson = (records, key) => { const record = payloadRecord(records, key); return record && record.t === 'json' ? record.v : null; };\nconst legacyKeys = new Set(['source_model_id', 'pin', 'route', 'reply_to', 'route.reply_to', 'return_topic', 'returnTopic', 'result_topic']);\nconst valueHasLegacy = (value, seen = new WeakSet()) => {\n  if (!value) return false;\n  if (Array.isArray(value)) {\n    if (seen.has(value)) return false;\n    seen.add(value);\n    return value.some((item) => valueHasLegacy(item, seen));\n  }\n  if (typeof value !== 'object') return false;\n  if (seen.has(value)) return false;\n  seen.add(value);\n  for (const [key, child] of Object.entries(value)) {\n    if (legacyKeys.has(key)) return true;\n    if (key === 'k' && typeof child === 'string' && legacyKeys.has(child)) return true;\n    if (valueHasLegacy(child, seen)) return true;\n  }\n  return false;\n};\nconst isSlideAppBundleResponsePayload = (records) => {\n  if (!Array.isArray(records) || records.length === 0 || !records.every(isRecord)) return false;\n  const kind = records.find((record) => isRecord(record) && record.id === 0 && record.p === 0 && record.r === 0 && record.c === 0 && record.k === '__mt_payload_kind');\n  return kind && kind.t === 'str' && kind.v === 'slide_app_bundle_response.v1';\n};\nconst recordsHaveLegacy = (records, options = {}) => {\n  if (!Array.isArray(records) || records.length === 0) return true;\n  if (!records.every(isRecord)) return true;\n  const allowSlideAppBundlePayload = options.allowSlideAppBundlePayload === true && isSlideAppBundleResponsePayload(records);\n  return records.some((record) => {\n    if (legacyKeys.has(record.k)) return true;\n    if (allowSlideAppBundlePayload && record.k === 'bundle_payload') {\n      if (!Array.isArray(record.v) || record.v.length === 0 || !record.v.every(isRecord)) return true;\n      return record.v.some((bundleRecord) => legacyKeys.has(bundleRecord.k));\n    }\n    return valueHasLegacy(record.v);\n  });\n};\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst endpointFrom = (records, prefix) => {\n  const endpoint = {\n    worker_id: payloadString(records, prefix + '_worker_id'),\n    model_id: payloadInt(records, prefix + '_model_id'),\n    pin: payloadString(records, prefix + '_pin'),\n  };\n  if (!safeSegment(endpoint.worker_id) || !Number.isInteger(endpoint.model_id) || endpoint.model_id <= 0 || !safeSegment(endpoint.pin)) return null;\n  return endpoint;\n};\nconst hasDuplicate = (records, keys) => {\n  const watched = new Set(keys);\n  const seen = new Set();\n  for (const record of records) {\n    if (!record || !watched.has(record.k)) continue;\n    if (seen.has(record.k)) return true;\n    seen.add(record.k);\n  }\n  return false;\n};\nconst parsePinPayload = (packet) => {\n  if (!packet || typeof packet !== 'object' || Array.isArray(packet)) return { ok: false, code: 'invalid_packet' };\n  const packetKeys = Object.keys(packet).sort().join(',');\n  if (packetKeys !== 'payload,type,version') return { ok: false, code: 'loose_pin_payload_fields_removed' };\n  if (packet.version !== 'v1' || packet.type !== 'pin_payload' || !Array.isArray(packet.payload)) return { ok: false, code: 'invalid_packet' };\n  const records = packet.payload;\n  if (records.length === 0 || !records.every(isRecord)) return { ok: false, code: 'temporary_modeltable_required' };\n  const metadataKeys = ['__mt_payload_kind', '__mt_request_id', 'op_id', 'message_role', 'endpoint_worker_id', 'endpoint_model_id', 'endpoint_pin', 'origin_worker_id', 'origin_model_id', 'origin_pin', 'reply_target_worker_id', 'reply_target_model_id', 'reply_target_pin', 'payload', 'timestamp', 'bus_out_key', 'bus'];\n  if (hasDuplicate(records, metadataKeys)) return { ok: false, code: 'invalid_pin_payload_records' };\n  for (const record of records) {\n    if (legacyKeys.has(record.k)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n    if (record.k !== 'payload' && valueHasLegacy(record.v)) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  }\n  const kind = payloadString(records, '__mt_payload_kind');\n  if (kind !== 'pin_payload.v1') return { ok: false, code: 'invalid_payload_kind' };\n  const requestId = payloadString(records, '__mt_request_id');\n  const opId = payloadString(records, 'op_id') || requestId;\n  if (!opId) return { ok: false, code: 'missing_request_correlation' };\n  const messageRole = payloadString(records, 'message_role');\n  if (messageRole !== 'request' && messageRole !== 'response') return { ok: false, code: 'invalid_message_role' };\n  const endpoint = endpointFrom(records, 'endpoint');\n  const origin = endpointFrom(records, 'origin');\n  const replyTarget = endpointFrom(records, 'reply_target');\n  const nestedPayload = payloadJson(records, 'payload');\n  if (!endpoint || !origin || !replyTarget || !Array.isArray(nestedPayload) || nestedPayload.length === 0 || !nestedPayload.every(isRecord)) return { ok: false, code: 'invalid_pin_payload_records' };\n  if (recordsHaveLegacy(nestedPayload, { allowSlideAppBundlePayload: true })) return { ok: false, code: 'legacy_pin_payload_metadata_removed' };\n  return { ok: true, records, endpoint, origin, replyTarget, nestedPayload, opId, messageRole };\n};\nconst pinBusValue = ({ opId, endpoint, origin, replyTarget, payload, timestamp = Date.now(), bus = '', messageRole = 'request' }) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', opId),\n    mt('op_id', 'str', opId),\n    mt('message_role', 'str', messageRole),\n    mt('endpoint_worker_id', 'str', endpoint.worker_id),\n    mt('endpoint_model_id', 'int', endpoint.model_id),\n    mt('endpoint_pin', 'str', endpoint.pin),\n    mt('origin_worker_id', 'str', origin.worker_id),\n    mt('origin_model_id', 'int', origin.model_id),\n    mt('origin_pin', 'str', origin.pin),\n    mt('reply_target_worker_id', 'str', replyTarget.worker_id),\n    mt('reply_target_model_id', 'int', replyTarget.model_id),\n    mt('reply_target_pin', 'str', replyTarget.pin),\n    mt('payload', 'json', payload),\n    mt('timestamp', 'int', timestamp),\n  ];\n  if (bus) records.push(mt('bus', 'str', bus));\n  return records;\n};\nconst writeControlOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', records);\nconst writeMgmtOut = (records) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', records);\nconst opId = 'mbr_ready_' + Date.now();\nmtAdd(-10, 0, 0, 0, 'mbr_ready_status', 'json', { op_id: opId, status: 'ready', ts: Date.now() });",
        "modelName": "mbr_role_v0"
      }
    }
  ]
}
