{
  "version": "mt.v0",
  "op_id": "mbr_role_v0_seed",
  "records": [
    {
      "op": "create_model",
      "model_id": -10,
      "name": "system",
      "type": "system"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "sys_worker_id",
      "t": "worker.id",
      "v": "5/10/28/35/14"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "sys_worker_role",
      "t": "worker.role",
      "v": "DEM"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_base",
      "t": "str",
      "v": "UIPUT/ws/dam/pic/de/sw"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_cb_out",
      "t": "pin.bus.cb.out",
      "v": null
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mb_out",
      "t": "pin.bus.mb.out",
      "v": null
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 1,
      "r": 0,
      "c": 0,
      "k": "model_type",
      "t": "model.submt",
      "v": -10
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_heartbeat_interval_ms",
      "t": "int",
      "v": 30000
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_matrix_event_filter",
      "t": "str",
      "v": "pin_payload"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_matrix_inbox_label",
      "t": "str",
      "v": "mbr_mgmt_inbox"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_matrix_func",
      "t": "str",
      "v": "mbr_mgmt_dispatch"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mgmt_dispatch",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst isRecord = (record) => record && typeof record === 'object' && !Array.isArray(record) && !hasOwn(record, 'op') && !hasOwn(record, 'model_id') && Number.isInteger(record.id) && Number.isInteger(record.p) && Number.isInteger(record.r) && Number.isInteger(record.c) && typeof record.k === 'string' && record.k.length > 0 && typeof record.t === 'string' && record.t.length > 0 && hasOwn(record, 'v');\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst pinBusValue = (packet) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', String(packet.op_id || '')),\n    mt('op_id', 'str', String(packet.op_id || '')),\n    mt('source_model_id', 'int', Number(packet.source_model_id || 0)),\n    mt('pin', 'str', String(packet.pin || '')),\n    mt('payload', 'json', Array.isArray(packet.payload) ? packet.payload : []),\n    mt('timestamp', 'int', Number.isFinite(Number(packet.timestamp)) ? Number(packet.timestamp) : Date.now()),\n  ];\n  const route = packet.route && typeof packet.route === 'object' && !Array.isArray(packet.route) ? packet.route : null;\n  if (route) records.push(mt('route', 'json', route));\n  return records;\n};\nconst writeControlOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', pinBusValue(packet));\nconst writeMgmtOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', pinBusValue(packet));\nconst inbox = mtRead(-10, 0, 0, 0, 'mbr_mgmt_inbox');\nif (!inbox || typeof inbox !== 'object' || Array.isArray(inbox)) return;\nconst sourceModelId = Number(inbox.source_model_id);\nconst payload = Array.isArray(inbox.payload) ? inbox.payload : [];\nconst read = (key, fallback = '') => { const record = payload.find((item) => isRecord(item) && item.id === 0 && item.p === 0 && item.r === 0 && item.c === 0 && item.k === key); return record && record.v != null ? String(record.v) : fallback; };\nconst cleanup = () => { mtDrop(-10, 0, 0, 0, 'mbr_mgmt_inbox'); };\nconst reject = (detail) => { mtAdd(-10, 0, 0, 0, 'mbr_mgmt_error', 'json', { op_id: String(inbox.op_id || ''), code: 'direct_model_mutation_disabled', detail }); cleanup(); };\nconst kind = read('__mt_payload_kind').trim();\nconst packetPin = String(inbox.pin || '').trim();\nconst target = read('target_user_id').trim();\nconst draft = (read('draft') || read('message_text')).trim();\nconst route = inbox.route && typeof inbox.route === 'object' && !Array.isArray(inbox.route) ? inbox.route : null;\nconst routeTo = route && route.to && typeof route.to === 'object' && !Array.isArray(route.to) ? route.to : null;\nconst replyTo = route && route.reply_to && typeof route.reply_to === 'object' && !Array.isArray(route.reply_to) ? route.reply_to : null;\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst routeToWorker = routeTo && typeof routeTo.worker_id === 'string' ? routeTo.worker_id.trim() : '';\nconst validRouteTo = routeTo && (routeToWorker === 'mbr' || routeToWorker === 'MBR') && Number(routeTo.model_id) === 1036 && routeTo.pin === 'submit';\nconst validReplyTo = replyTo && safeSegment(String(replyTo.worker_id || '')) && Number.isInteger(Number(replyTo.model_id)) && Number(replyTo.model_id) > 0 && safeSegment(String(replyTo.pin || ''));\nif (inbox.version === 'v1' && inbox.type === 'pin_payload' && sourceModelId === 1036) {\n  if (payload.length === 0 || !payload.every(isRecord)) { reject('temporary_modeltable_required'); return; }\n  if (packetPin === 'submit' && kind === 'mgmt_bus_console.send.v1' && target.startsWith('@mbr:') && draft) {\n    let opId = String(inbox.op_id || '');\n    if (!opId) opId = 'mgmt_bus_console_' + Date.now();\n    if (!validRouteTo) { reject('invalid_route_to'); return; }\n    if (!validReplyTo) { reject('missing_reply_to'); return; }\n    const seenKey = 'mbr_seen_' + opId;\n    if (mtRead(-10, 0, 0, 0, seenKey)) { cleanup(); return; }\n    mtAdd(-10, 0, 0, 0, seenKey, 'str', '1');\n    const reply = 'ack from ' + target + ': ' + draft;\n    const resultPayload = [\n      mt('__mt_payload_kind', 'str', 'mgmt_bus_console.ack.v1'),\n      mt('target_user_id', 'str', target),\n      mt('message_text', 'str', draft),\n      mt('reply_text', 'str', reply),\n      mt('source_op_id', 'str', opId),\n    ];\n    const replyRoute = {\n      to: { worker_id: String(replyTo.worker_id).trim(), model_id: Number(replyTo.model_id), pin: String(replyTo.pin).trim() },\n      from: { worker_id: 'MBR', model_id: 1036, pin: 'submit' },\n    };\n    writeMgmtOut({ version: 'v1', type: 'pin_payload', op_id: 'mbr_ack_' + opId, source_model_id: 1036, pin: String(replyTo.pin).trim(), payload: resultPayload, timestamp: Date.now(), route: replyRoute });\n    cleanup();\n    return;\n  }\n  reject('unsupported_mgmt_bus_console_payload');\n  return;\n}\nmtAdd(-10, 0, 0, 0, 'run_mbr_mgmt_to_mqtt', 'str', '1');",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mqtt_inbox_label",
      "t": "str",
      "v": "mbr_mqtt_inbox"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mqtt_func",
      "t": "str",
      "v": "mbr_mqtt_to_mgmt"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_ready_func",
      "t": "str",
      "v": "mbr_ready"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_heartbeat_func",
      "t": "str",
      "v": "mbr_heartbeat"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mgmt_to_mqtt",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst isRecord = (record) => record && typeof record === 'object' && !Array.isArray(record) && !hasOwn(record, 'op') && !hasOwn(record, 'model_id') && Number.isInteger(record.id) && Number.isInteger(record.p) && Number.isInteger(record.r) && Number.isInteger(record.c) && typeof record.k === 'string' && record.k.length > 0 && typeof record.t === 'string' && record.t.length > 0 && hasOwn(record, 'v');\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst pinBusValue = (packet) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', String(packet.op_id || '')),\n    mt('op_id', 'str', String(packet.op_id || '')),\n    mt('source_model_id', 'int', Number(packet.source_model_id || 0)),\n    mt('pin', 'str', String(packet.pin || '')),\n    mt('payload', 'json', Array.isArray(packet.payload) ? packet.payload : []),\n    mt('timestamp', 'int', Number.isFinite(Number(packet.timestamp)) ? Number(packet.timestamp) : Date.now()),\n  ];\n  const route = packet.route && typeof packet.route === 'object' && !Array.isArray(packet.route) ? packet.route : null;\n  if (route) records.push(mt('route', 'json', route));\n  return records;\n};\nconst writeControlOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', pinBusValue(packet));\nconst writeMgmtOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', pinBusValue(packet));\nconst inbox = mtRead(-10, 0, 0, 0, 'mbr_mgmt_inbox');\nif (!inbox || typeof inbox !== 'object' || Array.isArray(inbox)) return;\nlet opId = String(inbox.op_id || '');\nconst cleanup = () => { mtDrop(-10, 0, 0, 0, 'mbr_mgmt_inbox'); };\nconst reject = (detail) => {\n  mtAdd(-10, 0, 0, 0, 'mbr_mgmt_error', 'json', { op_id: opId, code: 'direct_model_mutation_disabled', detail });\n  cleanup();\n};\nif (inbox.version !== 'v1' || inbox.type !== 'pin_payload') {\n  reject('unsupported_bridge_message');\n  return;\n}\nconst sourceModelId = Number(inbox.source_model_id);\nif (!Number.isInteger(sourceModelId) || sourceModelId <= 0) {\n  reject('unsupported_source_model');\n  return;\n}\nconst route = inbox.route && typeof inbox.route === 'object' && !Array.isArray(inbox.route) ? inbox.route : null;\nconst routeTo = route && route.to && typeof route.to === 'object' && !Array.isArray(route.to) ? route.to : null;\nif (!routeTo) {\n  reject('missing_route_to');\n  return;\n}\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst workerId = typeof routeTo.worker_id === 'string' ? routeTo.worker_id : '';\nconst targetModelId = Number(routeTo.model_id);\nconst routePin = typeof routeTo.pin === 'string' ? routeTo.pin : '';\nconst packetPin = typeof inbox.pin === 'string' ? inbox.pin : '';\nif (!safeSegment(workerId) || !Number.isInteger(targetModelId) || targetModelId <= 0 || !safeSegment(routePin)) {\n  reject('invalid_route_to');\n  return;\n}\nif (routePin.includes(':')) {\n  reject('function_endpoint_not_allowed');\n  return;\n}\nif (!packetPin || packetPin !== routePin) {\n  reject('route_pin_mismatch');\n  return;\n}\nif (!Array.isArray(inbox.payload) || inbox.payload.length === 0 || !inbox.payload.every(isRecord)) {\n  reject('temporary_modeltable_required');\n  return;\n}\nif (!opId) opId = 'pin_' + Date.now();\nconst seenKey = 'mbr_seen_' + opId;\nif (mtRead(-10, 0, 0, 0, seenKey)) {\n  cleanup();\n  return;\n}\nmtAdd(-10, 0, 0, 0, seenKey, 'str', '1');\nwriteControlOut({\n  version: 'v1',\n  type: 'pin_payload',\n  op_id: opId,\n  source_model_id: sourceModelId,\n  pin: routePin,\n  payload: inbox.payload,\n  timestamp: typeof inbox.timestamp === 'number' && Number.isFinite(inbox.timestamp) ? inbox.timestamp : Date.now(),\n  route,\n});\ncleanup();",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_mqtt_to_mgmt",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst isRecord = (record) => record && typeof record === 'object' && !Array.isArray(record) && !hasOwn(record, 'op') && !hasOwn(record, 'model_id') && Number.isInteger(record.id) && Number.isInteger(record.p) && Number.isInteger(record.r) && Number.isInteger(record.c) && typeof record.k === 'string' && record.k.length > 0 && typeof record.t === 'string' && record.t.length > 0 && hasOwn(record, 'v');\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst pinBusValue = (packet) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', String(packet.op_id || '')),\n    mt('op_id', 'str', String(packet.op_id || '')),\n    mt('source_model_id', 'int', Number(packet.source_model_id || 0)),\n    mt('pin', 'str', String(packet.pin || '')),\n    mt('payload', 'json', Array.isArray(packet.payload) ? packet.payload : []),\n    mt('timestamp', 'int', Number.isFinite(Number(packet.timestamp)) ? Number(packet.timestamp) : Date.now()),\n  ];\n  const route = packet.route && typeof packet.route === 'object' && !Array.isArray(packet.route) ? packet.route : null;\n  if (route) records.push(mt('route', 'json', route));\n  return records;\n};\nconst writeControlOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', pinBusValue(packet));\nconst writeMgmtOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', pinBusValue(packet));\nconst inbox = mtRead(-10, 0, 0, 0, 'mbr_mqtt_inbox');\nif (!inbox || typeof inbox !== 'object' || Array.isArray(inbox)) return;\nconst payload = inbox.payload;\nconst cleanup = () => { mtDrop(-10, 0, 0, 0, 'mbr_mqtt_inbox'); };\nconst reject = (detail) => {\n  mtAdd(-10, 0, 0, 0, 'mbr_mqtt_error', 'json', { op_id: payload && typeof payload.op_id === 'string' ? payload.op_id : '', code: 'invalid_mqtt_pin_payload', detail });\n  cleanup();\n};\nif (!payload || typeof payload !== 'object' || Array.isArray(payload)) {\n  reject('unsupported_bridge_message');\n  return;\n}\nif (payload.version !== 'v1' || payload.type !== 'pin_payload') {\n  reject('unsupported_bridge_message');\n  return;\n}\nif (!Array.isArray(payload.payload) || payload.payload.length === 0 || !payload.payload.every(isRecord)) {\n  reject('temporary_modeltable_required');\n  return;\n}\nconst route = payload.route && typeof payload.route === 'object' && !Array.isArray(payload.route) ? payload.route : null;\nconst routeTo = route && route.to && typeof route.to === 'object' && !Array.isArray(route.to) ? route.to : null;\nif (!routeTo) {\n  reject('missing_route_to');\n  return;\n}\nconst safeSegment = (value) => typeof value === 'string' && value.trim() === value && value.length > 0 && !value.includes('/') && !value.includes('+') && !value.includes('#');\nconst targetModelId = Number(routeTo.model_id);\nconst routePin = typeof routeTo.pin === 'string' ? routeTo.pin : '';\nconst packetPin = typeof payload.pin === 'string' ? payload.pin : '';\nif (!safeSegment(String(routeTo.worker_id || '')) || !Number.isInteger(targetModelId) || targetModelId <= 0 || !safeSegment(routePin)) {\n  reject('invalid_route_to');\n  return;\n}\nif (!packetPin || packetPin !== routePin) {\n  reject('route_pin_mismatch');\n  return;\n}\nconst opId = String(payload.op_id || '');\nif (!opId) {\n  reject('missing_op_id');\n  return;\n}\nconst seenKey = 'mbr_seen_' + opId;\nif (mtRead(-10, 0, 0, 0, seenKey)) {\n  cleanup();\n  return;\n}\nmtAdd(-10, 0, 0, 0, seenKey, 'str', '1');\nwriteMgmtOut(payload);\ncleanup();",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_heartbeat",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst isRecord = (record) => record && typeof record === 'object' && !Array.isArray(record) && !hasOwn(record, 'op') && !hasOwn(record, 'model_id') && Number.isInteger(record.id) && Number.isInteger(record.p) && Number.isInteger(record.r) && Number.isInteger(record.c) && typeof record.k === 'string' && record.k.length > 0 && typeof record.t === 'string' && record.t.length > 0 && hasOwn(record, 'v');\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst pinBusValue = (packet) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', String(packet.op_id || '')),\n    mt('op_id', 'str', String(packet.op_id || '')),\n    mt('source_model_id', 'int', Number(packet.source_model_id || 0)),\n    mt('pin', 'str', String(packet.pin || '')),\n    mt('payload', 'json', Array.isArray(packet.payload) ? packet.payload : []),\n    mt('timestamp', 'int', Number.isFinite(Number(packet.timestamp)) ? Number(packet.timestamp) : Date.now()),\n  ];\n  const route = packet.route && typeof packet.route === 'object' && !Array.isArray(packet.route) ? packet.route : null;\n  if (route) records.push(mt('route', 'json', route));\n  return records;\n};\nconst writeControlOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', pinBusValue(packet));\nconst writeMgmtOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', pinBusValue(packet));\nconst opId = 'mbr_heartbeat_' + Date.now();\nmtAdd(-10, 0, 0, 0, 'mbr_heartbeat_status', 'json', { op_id: opId, status: 'ready', ts: Date.now() });",
        "modelName": "mbr_role_v0"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_ready",
      "t": "func.js",
      "v": {
        "code": "const host = ctx.hostApi;\nif (!host) throw new Error('host_api_missing');\nconst mtRead = (modelId, p, r, c, k, fallback = null) => {\n  const result = host.readCrossModel(modelId, p, r, c, k);\n  return result && result.ok && result.data && Object.prototype.hasOwnProperty.call(result.data, 'v') ? result.data.v : fallback;\n};\nconst mtAdd = (modelId, p, r, c, k, t, v) => {\n  const result = host.writeCrossModel(modelId, p, r, c, k, t, v);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_add_failed');\n  return result;\n};\nconst mtDrop = (modelId, p, r, c, k) => {\n  const result = host.rmCrossModel(modelId, p, r, c, k);\n  if (!result || result.ok !== true) throw new Error(result && result.code ? result.code : 'mt_remove_failed');\n  return result;\n};\nconst hasOwn = (obj, key) => Object.prototype.hasOwnProperty.call(obj, key);\nconst isRecord = (record) => record && typeof record === 'object' && !Array.isArray(record) && !hasOwn(record, 'op') && !hasOwn(record, 'model_id') && Number.isInteger(record.id) && Number.isInteger(record.p) && Number.isInteger(record.r) && Number.isInteger(record.c) && typeof record.k === 'string' && record.k.length > 0 && typeof record.t === 'string' && record.t.length > 0 && hasOwn(record, 'v');\nconst mt = (k, t, v) => ({ id: 0, p: 0, r: 0, c: 0, k, t, v });\nconst pinBusValue = (packet) => {\n  const records = [\n    mt('__mt_payload_kind', 'str', 'pin_payload.v1'),\n    mt('__mt_request_id', 'str', String(packet.op_id || '')),\n    mt('op_id', 'str', String(packet.op_id || '')),\n    mt('source_model_id', 'int', Number(packet.source_model_id || 0)),\n    mt('pin', 'str', String(packet.pin || '')),\n    mt('payload', 'json', Array.isArray(packet.payload) ? packet.payload : []),\n    mt('timestamp', 'int', Number.isFinite(Number(packet.timestamp)) ? Number(packet.timestamp) : Date.now()),\n  ];\n  const route = packet.route && typeof packet.route === 'object' && !Array.isArray(packet.route) ? packet.route : null;\n  if (route) records.push(mt('route', 'json', route));\n  return records;\n};\nconst writeControlOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_cb_out', 'pin.bus.cb.out', pinBusValue(packet));\nconst writeMgmtOut = (packet) => mtAdd(0, 0, 0, 0, 'mbr_mb_out', 'pin.bus.mb.out', pinBusValue(packet));\nconst opId = 'mbr_ready_' + Date.now();\nmtAdd(-10, 0, 0, 0, 'mbr_ready_status', 'json', { op_id: opId, status: 'ready', ts: Date.now() });",
        "modelName": "mbr_role_v0"
      }
    }
  ]
}
