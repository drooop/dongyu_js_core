apiVersion: v1
kind: Service
metadata:
  name: ui-server-nodeport
  namespace: dongyu
spec:
  type: NodePort
  selector:
    app: ui-server
  ports:
  - port: 9000
    targetPort: 9000
    nodePort: 30900
