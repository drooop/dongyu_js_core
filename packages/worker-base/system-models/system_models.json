{
  "version": "mt.v0",
  "op_id": "system_models_seed_v0",
  "records": [
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_mode",
      "t": "str",
      "v": "uiput_mm_v1"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_base",
      "t": "str",
      "v": "UIPUT/ws/dam/pic/de/sw"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_ns",
      "t": "str",
      "v": "UIPUT"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_ws",
      "t": "str",
      "v": "ws"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_dam",
      "t": "str",
      "v": "dam"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_pic",
      "t": "str",
      "v": "pic"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_de",
      "t": "str",
      "v": "de"
    },
    {
      "op": "add_label",
      "model_id": 0,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mqtt_topic_sw",
      "t": "str",
      "v": "sw"
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_route_100",
      "t": "json",
      "v": {
        "pin": "submit",
        "type": "pin_payload"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_route_1010",
      "t": "json",
      "v": {
        "pin": "submit",
        "type": "pin_payload"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_route_1019",
      "t": "json",
      "v": {
        "pin": "submit",
        "type": "pin_payload"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_route_default",
      "t": "json",
      "v": {
        "pin": "result",
        "type": "pin_payload"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mbr_default_model_id",
      "t": "int",
      "v": 2
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mgmt_send",
      "t": "func.js",
      "v": {
        "code": "const payload = ctx.getMgmtOutPayload('change_out') || ctx.getMgmtOutPayload(); if (payload) { return ctx.sendMatrix(payload); }",
        "modelName": "system_models"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "mgmt_receive",
      "t": "func.js",
      "v": {
        "code": "/* rubric: P10-write */ const inbox = ctx.getMgmtInbox(); if (!inbox || !inbox.k) return; const target = ctx.getMgmtInTarget(inbox.k); if (!target) return; let vv = inbox.v; if (inbox.t === 'json' && typeof inbox.v === 'string') { vv = ctx.parseJson(inbox.v); } ctx.hostApi.writeCrossModel(target.model_id, target.p, target.r, target.c, target.k, inbox.t, vv); ctx.clearMgmtInbox();",
        "modelName": "system_models"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "intent_dispatch",
      "t": "func.js",
      "v": {
        "code": "/* rubric: P4+P5+P10 (hostApi-only; programEngine path no V1N) */ const sys = ctx.runtime.getModel(-10); if (!sys) return; const cell = ctx.runtime.getCell(sys, 0, 0, 0); let nextKey = null; let nextId = null; let nextLabel = null; for (const [k, l] of cell.labels.entries()) { if (!k.startsWith('intent_job_')) continue; const id = Number(k.slice('intent_job_'.length)); if (!Number.isInteger(id)) continue; if (nextId === null || id < nextId) { nextId = id; nextKey = k; nextLabel = l; } } if (!nextKey || !nextLabel) return; const job = nextLabel.v; const source = job && job.source ? job.source : null; const intent = job && job.intent ? job.intent : null; const sourceModelId = source && Number.isInteger(source.model_id) ? source.model_id : null; const sourceRef = source && Number.isInteger(source.p) && Number.isInteger(source.r) && Number.isInteger(source.c) && typeof source.k === 'string' ? source : null; const opId = intent && typeof intent.op_id === 'string' ? intent.op_id : ''; const action = intent && typeof intent.action === 'string' ? intent.action : ''; function ok(extra) { if (sourceRef) { ctx.hostApi.writeCrossModel(sourceRef.model_id, sourceRef.p, sourceRef.r, sourceRef.c, 'intent_result', 'json', Object.assign({ op_id: opId, result: 'ok', action }, extra || {})); } ctx.hostApi.rmCrossModel(-10, 0, 0, 0, nextKey); } function fail(code, detail) { ctx.hostApi.writeCrossModel(-10, 0, 0, 0, 'intent_error', 'json', { op_id: opId, code, detail, job: nextKey }); if (sourceRef) { ctx.hostApi.writeCrossModel(sourceRef.model_id, sourceRef.p, sourceRef.r, sourceRef.c, 'intent_result', 'json', { op_id: opId, result: 'error', code, detail }); } ctx.hostApi.rmCrossModel(-10, 0, 0, 0, nextKey); return; } if (!sourceModelId || sourceModelId <= 0) return fail('invalid_source', 'source.model_id must be > 0'); if (!opId || !action) return fail('invalid_intent', 'missing op_id/action'); const dedupeKey = `intent_last_op_id_${sourceModelId}`; const _lastRead = ctx.hostApi.readCrossModel(-10, 0, 0, 0, dedupeKey); const last = _lastRead && _lastRead.ok && _lastRead.data ? _lastRead.data.v : null; if (last && last === opId) { ctx.hostApi.rmCrossModel(-10, 0, 0, 0, nextKey); return; } ctx.hostApi.writeCrossModel(-10, 0, 0, 0, dedupeKey, 'str', opId); if (action === 'mgmt_bind_in') { const channel = typeof intent.channel === 'string' ? intent.channel.trim() : ''; const target = intent && typeof intent.target_ref === 'object' ? intent.target_ref : null; if (!channel) return fail('invalid_channel', 'missing channel'); if (!target || !Number.isInteger(target.model_id) || !Number.isInteger(target.p) || !Number.isInteger(target.r) || !Number.isInteger(target.c) || typeof target.k !== 'string' || !target.k) return fail('invalid_target', 'target_ref invalid'); ctx.hostApi.writeCrossModel(-10, 0, 0, 1, channel, 'MGMT_IN', target); ok({ channel }); return; } if (action === 'mgmt_publish') { const channel = typeof intent.channel === 'string' ? intent.channel.trim() : 'change_out'; const payload = Object.prototype.hasOwnProperty.call(intent, 'payload') ? intent.payload : null; if (!payload || typeof payload !== 'object') return fail('invalid_payload', 'missing payload'); ctx.hostApi.writeCrossModel(-10, 0, 0, 2, channel, 'MGMT_OUT', payload); ok({ channel }); return; } if (action === 'mgmt_put_mbr_v0') { const base = String(((ctx.hostApi.readCrossModel(0, 0, 0, 0, 'mqtt_topic_base') || {}).data || {}).v || '').trim(); if (!base) return fail('missing_topic_base', 'mqtt_topic_base required'); const cellK = typeof intent.cell_k === 'string' ? intent.cell_k.trim() : ''; if (!cellK) return fail('invalid_cell_k', 'missing cell_k'); const t = typeof intent.t === 'string' ? intent.t : 'json'; const raw = Object.prototype.hasOwnProperty.call(intent, 'v') ? intent.v : null; let vv = raw; if (t === 'json') { if (typeof raw === 'string') { vv = raw; } else { try { vv = JSON.stringify(raw); } catch (_) { return fail('invalid_json', 'v not json-serializable'); } } } else { vv = typeof raw === 'string' ? raw : String(raw); } const topic = `${base}/${sourceModelId}/${cellK}`; const payload = { topic, signature: opId, op_id: opId, k: cellK, t, v: vv, timestamp: String(Date.now()), channel: cellK }; ctx.hostApi.writeCrossModel(-10, 0, 0, 2, cellK, 'MGMT_OUT', payload); ok({ channel: cellK, topic }); return; } return fail('unknown_action', action);",
        "modelName": "system_models"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "pin_demo_set_mqtt_config",
      "t": "func.js",
      "v": {
        "code": "/* rubric: R14 setMqttTargetConfig system bridge */ const host = String(ctx.getState('pin_demo_host') || '').trim(); const port = ctx.getStateInt('pin_demo_port'); const clientId = String(ctx.getState('pin_demo_client_id') || '').trim(); if (!host || !port || !clientId) return; ctx.hostApi.setMqttTargetConfig(host, port, clientId);",
        "modelName": "system_models"
      }
    },
    {
      "op": "add_label",
      "model_id": -10,
      "p": 0,
      "r": 0,
      "c": 0,
      "k": "pin_demo_start_mqtt_loop",
      "t": "func.js",
      "v": {
        "code": "ctx.startMqttLoop();",
        "modelName": "system_models"
      }
    }
  ]
}
